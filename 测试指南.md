# AI ChatBot Mod - 测试指南

## 📋 项目构建状态
✅ 编译成功！生成的 JAR 文件位置：`build/libs/aichatbot-1.0.0.jar`

## 🎮 测试方法

### 方法 1：运行开发服务器（推荐）
```bash
cd c:\scist_camp\CTF
.\gradlew.bat runServer
```
这会启动一个带有你的 mod 的本地 Minecraft 服务器。

### 方法 2：运行开发客户端
```bash
cd c:\scist_camp\CTF
.\gradlew.bat runClient
```
这会启动 Minecraft 游戏客户端。

### 方法 3：手动安装到现有 Minecraft
1. 找到生成的 mod JAR 文件：
   ```
   c:\scist_camp\CTF\build\libs\aichatbot-1.0.0.jar
   ```
2. 将其复制到你的 Minecraft `.minecraft/mods` 文件夹
3. 确保你已安装 Fabric Loader 和 Fabric API

## ⚙️ 配置 API Key
1. 第一次运行时，mod 会自动生成配置文件：
   ```
   .minecraft/config/aichatbot.json
   ```
2. 打开并修改：
   ```json
   {
     "apiKey": "你的 OpenAI API KEY",
     "model": "gpt-3.5-turbo",
     "prefix": "!ai",
     "temperature": 0.7
   }
   ```

## 🧪 在游戏中测试
1. 进入游戏
2. 输入命令：`!ai 你好`
3. AI 应该会回复你的问题

## 🐛 调试
- 查看日志：`logs/latest.log`
- 常见问题：
  - ❌ API 失败 → 检查 API KEY 是否正确
  - ❌ 命令无响应 → 检查前缀是否为 `!ai`
  - ❌ 网络错误 → 检查网络连接和防火墙

---

**建议从方法 1 (runServer) 开始，因为它最简单！**
